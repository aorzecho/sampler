## Stream sampler generating random representative sample - go implementation --WiP--

See Makefile for how it is built by default.

### building
Requires: go 1.6

Build a binary required for [sampler.sh](../) script, using go building tools.
```
go install ./...
```
